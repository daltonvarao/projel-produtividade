@layout('layouts/master')

@set('title', 'Furos')

@section('content')
  <div class="form-container">
    <div class="card-form">
      <form action="{{ route('admin.areas.furos.store') }}" method="post">
        <h2>Novo Furo</h2>
        @!component('components/input', {
          name: 'nome',
          label: 'Nome',
          value: flashMessages.get('nome') || '',
          error: 'errors.nome'
        })

        @!component('components/select', {
          name: 'estrutura_id',
          label: 'Estrutura',
          valueKey: 'id',
          labelKey: 'nome',
          collection: estruturas,
          value: flashMessages.get('estrutura_id') || '',
          error: 'errors.estrutura_id'
        })

        <button class="btn btn-primary">Salvar</button>
      </form>
    </div>
  </div>
@endsection
