<div class="card-info">
  <h2 class="card-title">
    {{ title }}

    <div class="card-title-actions">
      {{{ $slots.actions ? await $slots.actions() : '' }}}
    </div>
  </h2>
  <div class="card-body">
    {{{ await $slots.content() }}} 
  </div>
</div>